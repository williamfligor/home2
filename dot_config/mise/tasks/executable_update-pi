#!/usr/bin/env bash
#MISE description="Update pi, extensions, and install extension deps"
set -euxo pipefail

pi update
# Show what is about to change before installing pending extension updates
mise run update-pi-summary || true
pi update --extensions
bash "$HOME/.local/share/chezmoi/.chezmoiscripts/run_onchange_after_99-pi-extensions.sh"
