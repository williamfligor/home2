#!/usr/bin/env bash
#MISE description="Update pi, extensions, and install extension deps"
set -euxo pipefail

pi update
pi update --extensions
# Install any new extension dependencies (the post-tools bootstrap hook does this
# on bootstrap; this covers the case where an extension changed post-bootstrap).
"$HOME/.local/bin/pi-ext-deps"
